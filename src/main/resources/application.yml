server:
  port: 8888
#  context-path: /sso
#  address: sso.springboot解析httpsserver

#spring:
#  mvc:
#    static-path-pattern: /static/**
    # ???? classpath:/META-INF/resources/,classpath:/resources/,classpath:/static/,classpath:/public/
#  ssl:
#    key-store : classpath:keystore.p12       #（密钥文件路径，也可以配置绝对路径）
#    key-store-password : 123456             #（密钥生成时输入的密钥库口令）
#    keyStoreType : PKCS12              #（密钥类型，与密钥生成命令一致）
#    keyAlias : tomcat                   # （密钥别名，与密钥生成命令一致）

#  ssl:
#    key-store : classpath:oldestdriver.com.jks
#    key-store-password : s1rli7oxwerw
#    key-password : "**HFhf0215128178"
thymeleaf:
  prefix: classpath:/templates/  #prefix：指定模板所在的目录
  check-template-location: true  #check-tempate-location: 检查模板路径是否存在
  cache: false  #cache: 是否缓存，开发模式下设置为false，避免改了模板还要重启服务器，线上设置为true，可以提高性能。
  suffix:  .html
  #encoding: UTF-8
  #content-type: text/html
  mode: HTML5
